source('https://rubygems.org')
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '3.3.3'

gem 'pry'

gem 'terminal-table'
gem 'colorize'

group :test do
  gem 'rspec'
  gem 'simplecov'
end
